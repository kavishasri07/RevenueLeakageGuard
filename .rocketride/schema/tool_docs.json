{
  "actions": 0,
  "capabilities": 3072,
  "classType": [
    "tool"
  ],
  "description": "Exposes Google Docs operations as agent tools.\nRead a document's text; create documents; append and replace text; insert images and tables; and run arbitrary batchUpdate requests (styling, named ranges, positioned inserts).\nAuthenticates via a Google service account or user OAuth.",
  "documentation": "https://docs.rocketride.org",
  "icon": "docs.svg",
  "lanes": {},
  "Pipe": {
    "schema": {
      "dependencies": {
        "authType": {
          "oneOf": [
            {
              "properties": {
                "adminEmail": {
                  "description": "Enter the email address of a Google Workspace administrator.\n\nThis email should belong to a user with administrative privileges in your Google Workspace domain.",
                  "title": "Administrator E-mail",
                  "type": "string"
                },
                "authType": {
                  "enum": [
                    "service"
                  ]
                },
                "customerId": {
                  "description": "Enter your Google Workspace Customer ID.\n\nThis unique identifier is assigned to your organization by Google. It is used to specify the particular Google Workspace domain you want to manage.",
                  "title": "Customer ID",
                  "type": "string"
                },
                "serviceKey": {
                  "description": "Upload the JSON key file for your Google Workspace service account.\n\nThis file contains the credentials necessary to authenticate API requests.",
                  "format": "data-url",
                  "title": "Service Account Key File",
                  "type": "string"
                }
              }
            },
            {
              "properties": {
                "authType": {
                  "enum": [
                    "user"
                  ]
                },
                "oAuthButton": {
                  "title": "Login with Google",
                  "type": "string"
                },
                "userToken": {
                  "description": "It is a long term token that allows you to get new access tokens to access the Google API.",
                  "title": "Access Token",
                  "type": "string"
                }
              }
            }
          ]
        }
      },
      "properties": {
        "access": {
          "default": "write",
          "description": "Docs scopes to request. readonly: read document text and metadata only. write: full read/write (create documents, append and replace text, insert images and tables, and run arbitrary batchUpdate requests).",
          "enum": [
            "readonly",
            "write"
          ],
          "title": "Access level",
          "type": "string"
        },
        "authType": {
          "default": "service",
          "enum": [
            "service",
            "user"
          ],
          "title": "Authentication Type",
          "type": "string"
        },
        "type": {
          "default": "tool_docs",
          "type": "string"
        }
      },
      "required": [
        "type",
        "authType",
        "access"
      ],
      "title": "Google Docs",
      "type": "object"
    },
    "ui": {
      "access": {
        "ui:enumNames": [
          "Read-only",
          "Read/write"
        ]
      },
      "adminEmail": {},
      "authType": {
        "ui:enumNames": [
          "Service",
          "User"
        ],
        "ui:readonlyOnEdit": true
      },
      "customerId": {},
      "oAuthButton": {
        "ui:widget": "GoogleButtonWidget"
      },
      "serviceKey": {
        "ui:options": {
          "accept": ".json"
        }
      },
      "type": {
        "ui:options": {
          "label": false
        },
        "ui:widget": "hidden"
      },
      "ui:order": [
        "type",
        "authType",
        "customerId",
        "adminEmail",
        "serviceKey",
        "oAuthButton",
        "userToken",
        "access"
      ],
      "userToken": {
        "ui:disabled": true,
        "ui:widget": "password"
      }
    }
  },
  "plans": null,
  "prefix": "docs",
  "protocol": "tool_docs://",
  "tile": [
    "Google Docs (${parameters.docs.access})"
  ],
  "title": "Google Docs"
}